Use this file only as a reference. Do not put private passwords in public screenshots.

Local MongoDB:
MONGODB_URI=mongodb://127.0.0.1:27017
DB_NAME=shopease

MongoDB Atlas example:
MONGODB_URI=mongodb+srv://username:password@cluster-name.mongodb.net/?retryWrites=true&w=majority
DB_NAME=shopease

If the password contains @, write it as %40 inside the connection string.
